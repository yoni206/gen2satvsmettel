/**
 * This file is generated by MetTeL v2
 * which is designed and implemented
 * by Dmitry Tishkovsky.
 */
package Lukavronb.language.Lukavronb;

import java.util.Comparator;

import mettel.core.tableau.MettelExpression;

public class Lukavronb_formulaVariable extends LukavronbAbstractVariable implements Lukavronb_formula{

    public Lukavronb_formulaVariable(String name, LukavronbObjectFactory factory) {
        super(name, factory);
    }

    int sortId(){ return SORTID; }

    public LukavronbSubstitution match(LukavronbExpression e){
        LukavronbSubstitution s = new LukavronbTreeSubstitution();
        if(match(e,s)){
            return factory.getSubstitution(s);
        }else{ return null; }
    }

    public boolean match(LukavronbExpression e, LukavronbSubstitution s){
        if(!(e instanceof Lukavronb_formula)){ return false; }
        return s.append(this,(Lukavronb_formula)e);
    }

    public LukavronbExpression rewrite(LukavronbReplacement r) {
        Lukavronb_formula e = r.get_formula(this);
        return (e == null) ? this : e;
    }

    public LukavronbExpression substitute(LukavronbSubstitution s) {
        Lukavronb_formula e = s.get_formula(this);
        return (e == null) ? this : e;
    }

    private int hashCode = 0;
    public int hashCode(){
        if(hashCode == 0){
            hashCode = 23*SORTID + name.hashCode();
        }
        return hashCode;
    }

    public int compareTo(MettelExpression e){
        if(e instanceof Lukavronb_formulaVariable) return name.compareTo(((Lukavronb_formulaVariable)e).name());
        if(e instanceof Lukavronb_formula) return -1;
        return SORTID - ((LukavronbAbstractExpression)e).sortId();
    }

    int compareArgumentsTo(LukavronbAbstractExpression e, Comparator<LukavronbAbstractExpression> c){return 0;}
    int compareArguments(LukavronbAbstractExpression e, Comparator<LukavronbAbstractExpression> c){return 0;}

    public boolean equals(Object o){
        if(o == this){ return true; }
        if(!(o instanceof Lukavronb_formulaVariable)){ return false; }
        return name.equals(((Lukavronb_formulaVariable)o).name());
    }

    public boolean isEquality(){
        return false;
    }
    public int length(){
        return 1;
    }
}
